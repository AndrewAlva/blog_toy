<%= form_for [object, Comment.new], remote: true do |f| %>
  <%= f.label :content, "Escribir nuevo comentario" %><br/>
  <%= f.text_area :content, placeholder: "Esto es un comentario...", :class => "comments_textarea" %>
  <p><%= f.submit "Comentar" %></p>
<% end %>